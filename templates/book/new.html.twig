{% extends 'base.html.twig' %}

{% block title %}Ajouter un livre{% endblock %}

{% block body %}
    <section class="container-box py-12">
        <div class="max-w-2xl mx-auto bg-white rounded-xl shadow-md p-6 sm:p-8 border border-gray-100">
            <h1 class="text-2xl font-bold text-slate-800 mb-6">Ajouter un nouveau livre</h1>

            {{ include('book/_form.html.twig', {'button_label': 'Ajouter mon livre'}) }}

            <div class="flex justify-between items-center mt-6 border-t pt-4">
                <a href="{{ path('app_home') }}" class="text-sm text-blue-600 hover:underline">
                    ← Retour à l'accueil
                </a>
            </div>
        </div>
    </section>
{% endblock %}
