{% extends 'base.html.twig' %}

{% block title %}Créer une nouvelle catégorie{% endblock %}

{% block body %}
    <section class="container-box py-12">
        <div class="max-w-xl mx-auto bg-white rounded-xl shadow-md p-6 sm:p-8 border border-gray-100">
            <h1 class="text-2xl font-bold text-slate-800 mb-6">Créer une nouvelle catégorie</h1>

            {{ include('category/_form.html.twig', {'button_label': 'Créer'}) }}

            <div class="mt-6 border-t pt-4">
                <a href="{{ path('app_category_index') }}"
                   class="text-sm text-blue-600 hover:underline">
                    ← Retour à la liste
                </a>
            </div>
        </div>
    </section>
{% endblock %}
